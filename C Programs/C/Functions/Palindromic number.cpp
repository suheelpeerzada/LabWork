#include<stdio.h>
main()
{
	int n, num, i;
	printf("How many digit number is desired (only odds)?:\n");
	scanf("%d", &num);
	if (num/2==0)
	{
		printf("Please enter an odd number\n");
		return 0;
	}
	else ()continue;
	for(i=0;i<=n;i++)
	{
		
	}
}
